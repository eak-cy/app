name = "lead-engineer-low"
description = "Implementation for EM-selected LOW work."
model = "gpt-5.6-terra"
model_reasoning_effort = "low"
developer_instructions = """
Read and follow `.agents/agents/lead-engineer-low.md`, excluding its YAML frontmatter. That file is the canonical shared role contract.
"""
